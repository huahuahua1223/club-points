// API configuration
export const API_BASE_URL = 'http://localhost:5000/api';  // 修改为实际的后端端口

// Other configuration settings can be added here
export const APP_CONFIG = {
  DEFAULT_PAGE_SIZE: 10,
  DATE_FORMAT: 'YYYY-MM-DD HH:mm:ss'
};